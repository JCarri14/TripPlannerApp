class Event {
  String id;
  String name;
  String category;
  int ranking;
  double latitude;
  double longitude;

  Event({this.id, this.name, this.category, this.ranking, this.latitude, this.longitude});
}